<% content_for(:title, "Modifier - #{@property.title}") %>
<div class="max-w-3xl mx-auto">
  <h1 class="text-xl sm:text-2xl font-bold text-gray-900 mb-4 sm:mb-6 truncate">Modifier : <%= @property.title %></h1>
  <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-4 sm:p-6">
    <%= render "form", property: @property %>
  </div>
</div>
